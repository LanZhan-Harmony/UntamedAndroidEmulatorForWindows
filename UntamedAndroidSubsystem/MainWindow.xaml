<?xml version="1.0" encoding="utf-8" ?>
<Window x:Class="UntamedAndroidSubsystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="using:UntamedAndroidSubsystem"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="UntamedAndroidSubsystem"
        mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TitleBar x:Name="titleBar"
                  Grid.Row="0"
                  Height="48"
                  IsBackButtonEnabled="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}"
                  IsBackButtonVisible="True"/>
        <NavigationView Grid.Row="1" IsBackButtonVisible="Collapsed">
            <NavigationView.MenuItems>
                <NavigationViewItem/>
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</Window>
