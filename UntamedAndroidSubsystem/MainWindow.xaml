<?xml version="1.0" encoding="utf-8" ?>
<winuiex:WindowEx x:Class="UntamedAndroidSubsystem.MainWindow"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:local="using:UntamedAndroidSubsystem"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:ui="using:CommunityToolkit.WinUI"
                  xmlns:winuiex="using:WinUIEx"
                  MinWidth="540"
                  mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TitleBar x:Name="titleBar" x:Uid="Main_TitleBarDisplayName"
                  Grid.Row="0"
                  Height="48"
                  IsBackButtonEnabled="{x:Bind ContentFrame.CanGoBack, Mode=OneWay}"
                  IsBackButtonVisible="True">
            <TitleBar.LeftHeader>
                <Image Width="16"
                       Margin="6,0,10,0"
                       AutomationProperties.AccessibilityView="Raw"
                       Source="ms-appx:///Assets/Icon.ico"/>
            </TitleBar.LeftHeader>
        </TitleBar>
        <NavigationView x:Name="NavView"
                        Grid.Row="1"
                        IsBackButtonVisible="Collapsed"
                        IsSettingsVisible="False"
                        ItemInvoked="NavView_ItemInvoked"
                        Loaded="NavView_Loaded" PaneDisplayMode="Auto">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="AndroidDevices"
                                    Icon="{ui:FontIcon Glyph=&#xE8CC;}"
                                    Tag="Device"/>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Uid="Settings" Tag="Settings">
                    <NavigationViewItem.Icon>
                        <AnimatedIcon x:Name="AnimatedIcon">
                            <AnimatedIcon.Source>
                                <animatedvisuals:AnimatedSettingsVisualSource/>
                            </AnimatedIcon.Source>
                            <AnimatedIcon.FallbackIconSource>
                                <FontIconSource Glyph="&#xE713;"/>
                            </AnimatedIcon.FallbackIconSource>
                        </AnimatedIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            <Frame x:Name="ContentFrame"/>
        </NavigationView>
    </Grid>
</winuiex:WindowEx>
